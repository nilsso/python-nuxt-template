// https://github.com/RobertCraigie/prisma-client-py
generator python {
    provider                    = "prisma-client-py"
    enable_experimental_decimal = true
    // -1 for full recursive types (bad with Pydantic though)
    recursive_type_depth        = 3
}
